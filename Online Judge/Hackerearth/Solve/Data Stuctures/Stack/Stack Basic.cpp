#include<bits/stdc++.h>
using namespace std;

int stack[100];
int top_id=-1;

void push(int n)
{
    if(top_id==n-1)
        cout << "Stack is full. Overflow condition."
    else
    {
        top++;
        stack[top]=n;
    }
}

void pop()
{
    if(top_id==)
}

int main()
{




    return 0;
}
