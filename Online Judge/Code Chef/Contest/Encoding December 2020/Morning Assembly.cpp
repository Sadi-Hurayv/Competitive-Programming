#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long long int t, r, c;
    cin >> t;
    while(t--)
    {
        cin >> r >> c;
        cout << r*c << "\n";
    }

    return 0;
}
